# The user's diary — personal data, never goes into the repository
.diary/
*.hsam.json
journal.jsonl
links.jsonl
attachments/
last_voice.wav

# Keys
key
groq_key
.env
*.key

# Build artefacts
build/
dist/
*.spec.bak
__pycache__/
*.pyc

# The engine library is built in CI for the right platform
diary/lib/*.dylib
diary/lib/*.so
diary/lib/*.dll

# Local
.DS_Store
*.log

# Design notes and task briefs contain details about the people this was made for.
# They are not needed for the build and do not go into the repository — they stay local.
docs/
